name: build
on: push
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - run: sudo apt install -y libev-dev libssl-dev
      - uses: actions/checkout@v2
        with:
          submodules: true
      - run: cmake .
      - run: make
